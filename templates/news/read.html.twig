{% extends 'base.html.twig' %}

{% block title %}News{% endblock %}

{% set descriptionBr = read.description|replace({'\\n': ''}) %}

{% block body %}

    {{ include('shared/navbar.html.twig') }}
    <div class="container">
    <div class="row">
    <div class="d-flex flex-column justify-content-center">
        <img src="{{ asset('build/images/' ~ read.poster) }}" class="article-img col-11 col-lg-8 mx-auto mt-4" />
        <h2 class="article-title text-light d-flex justify-content-center mt-3">{{read.title}}</h2>
        <p class="article-description text-light mx-auto mt-3 fs-5">{{ descriptionBr|nl2br }}</p>   
        <p class="article-date text-light mx-auto">publié le {{read.postdate.format('d-m-Y')}}</p>      
    </div>
    </div>
    </div>

{% endblock %}